
<% if trip_creator(admin) %>
	<li><em><%= admin.username %> (trip creator)</em></li>
<% else %>
	<li id="admin-user-<%= admin.id %>">
		<%= admin.username %> | 
		<a class="remove-admin" data-id=<%= "#{admin.id}" %> >remove</a>
	</li>

<% end %>